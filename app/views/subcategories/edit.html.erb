<h1>Editing Subcategory</h1>

<%= render 'form', subcategory: @subcategory %>

<%= link_to 'Show', @subcategory %> |
<%= link_to 'Back', {:action=>:show, :controller=>:categories, :id=>(@category.id)} %>
